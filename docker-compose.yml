# Use root/example as user/password credentials
version: '3.1'

services:
  mongo:
    image: mongo:4.4.20-focal
    expose:
      - '27017'
    volumes:
      - dbdata6:/data/db

volumes:
  dbdata6:
